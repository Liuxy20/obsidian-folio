# 版本记录

## 0.4.7 — 2026-09-20 · 公开测试版

- 补齐首次点选正文后浮窗的完整 Markdown 原文预览，移除 90 字纯文本截断；支持双向滚动和拖动调高。
- 选中与悬停描边改为向内绘制，避免表格滚动容器裁掉左侧绿色边线。
- 浮窗尺寸变化后重新约束在阅读视口中，关闭时清理尺寸监听。

## 0.4.6 — 2026-09-20 · 公开测试版

本次从 0.3.5 升级，包含下列 0.4.0–0.4.5 本地开发记录中的全部更新。

- 侧栏关联原文与卡片共用完整 Markdown 预览，支持双向滚动、拖动调高，保留定位正文入口。
- 卡片明确区分继续提问、让 AI 重答、修改笔记原文；重答要求可留空，旧回答作为历史保留，笔记不变。
- 重答模式与要求随草稿保存，停止或失败后可重试；侧栏回答也提供重答入口，发送前可调整留言。

## 0.4.5 — 本地开发记录，随 0.4.6 发布

- 卡片修改模式的关联原文按 Markdown 渲染，完整显示选中段落，不再截断到 140 字。
- 原文预览支持纵向滚动、宽表格横向滚动，以及拖动右下角调整高度。

## 0.4.4 — 本地开发记录，随 0.4.6 发布

- 卡片内可切换提问或修改，显示关联原文与保留数字选项；生成的修改通过弹窗预览、确认采用后保存，可撤销。
- 选中当前或历史 AI 回答片段后点“引用追问”，标明回答轮次；引用与模式随草稿保存，旧文本草稿仍兼容。
- 发送前校验引用所属讨论与原回答，引用最多 2000 字，仅作为参考；修改目标仍为关联笔记原文。历史与导出保留引用关系。

## 0.4.3 — 本地开发记录，随 0.4.6 发布

- 导出讨论先展示保存弹窗，默认原笔记目录与可读文件名；可选目录、修改文件名或创建新目录，实时预览最终路径。
- 确认后才创建笔记；取消不写文件，同名文件与无效路径会提示，跨目录导出保留正确的原笔记链接。

## 0.4.2 — 本地开发记录，随 0.4.6 发布

- Markdown 讨论取消本地 30 条记录及 8 轮历史截断；显示分页与模型上下文上限分别处理，旧版已淘汰内容不自动恢复。
- 历史列表每次加载 30 条，保持旧上新下；完整讨论可导出为原笔记旁的新 Markdown 文件。原文、问答以文本块保存，避免导出后运行代码或加载嵌入内容。
- 长回答随正文滚动、卡片适配正文宽度；正文高亮与浮动操作位置不再包含讨论区。
- 历史默认展开，用户可主动收起；卡片与历史展开状态在本地保存，重载后恢复。

## 0.4.1 — 本地开发记录，随 0.4.6 发布

- 正文按讨论创建时间从旧到新展示，追问保持在同一线程下方；历史问答直接可见。
- 修复从旧版升级后，已发送的残留输入被误建为“未发送”草稿。

## 0.4.0 — 本地开发记录，随 0.4.6 发布

- 卡片内追问：输入框原地展开、按讨论保存草稿、生成时停止、失败重试；侧栏按需打开与收起。
- Markdown AI 审稿：发送范围预览、最多 5 条原文批注，先讨论后修改。
- 同段连续追问、解决/重开、唯一原文定位与手动重新关联，重命名保留记录。
- 多处意见集中生成、逐项勾选、完整正文预览、统一保存与整批撤销。
- 保存中断恢复，保留版本/重叠/编辑器冲突保护；旧记录兼容。
- 安装包附带隐私说明；SHA256 文件采用标准校验格式。

## 0.3.5 — 2026-09-18

- 左侧两个图标合并为一个「页间：批注当前文档」，自动识别当前 Markdown 或 HTML。
- 已打开的 HTML 工作台直接复用，重复点击保留界面和草稿；通用打开命令使用相同行为。
- 空白页进入统一文档首页，可打开笔记、选择库内 HTML 或导入 HTML；保留独立快捷命令。

## 0.3.4 — 2026-09-17

- 段落留言卡片与侧栏回答支持 Markdown 渲染，包括强调、列表、表格和代码块。
- 历史回答直接以新格式展示，复制仍保留原始 Markdown；代码与表格可横向滚动。
- 使用独立渲染器，不运行笔记插件代码块，不加载内嵌图片，HTML 作为文字显示。

## 0.3.3 — 2026-09-17

- 笔记入口直接使用当前文档，移除必经文件选择器；文件查找保留为独立命令。
- 侧栏跟随当前标签与同标签文件切换；阅读视图自动点选，编辑视图保留编辑模式，各篇草稿独立恢复。
- 切换时停止旧笔记生成并保留留言，提交前再次确认当前文档，防止使用旧上下文。
- 无文档或 Excalidraw 等不支持类型显示说明；自动跟随不抢焦点，关闭侧栏不自动重开。

## 0.3.2 — 2026-09-17

- 段落旁原地输入提问或修改，支持自动保存草稿、快捷键发送和 Esc 收起。
- 提交后立即保留留言卡片，展开查看回答；关闭面板、重开笔记和插件重载后恢复。
- 失败问题保留，修改卡片连接审阅流程；卡片仅存于插件数据，不写入 Markdown。
- 使用原文版本及源码行挂靠；笔记变动后旧留言留在侧栏历史，避免误贴到其他段落。
- 清理阅读视图重复渲染和插件停用时的卡片，提交过的留言不再重复存为未发送草稿。

## 0.3.1 — 2026-09-17

- Markdown 阅读视图正文点选，悬停描边、点击高亮、就近修改/提问工具条。
- 使用原生 section 行映射，支持重复段落、列表、表格、callout；排除其他笔记嵌入。
- 引用定位改为正文高亮，侧栏缩短原文摘录，保留编辑器精确选区。

## 0.3.0 — 2026-09-17

- Markdown 原生编辑器选区或段落留言，支持修改与提问。
- 修改差异审阅、采用保存、备份撤销；问答附笔记行号引用，不改变原文。
- 笔记选择入口、编辑器右键菜单、命令面板和右侧留言历史。
- 未发送留言与结果持久化、取消生成、原文和编辑器冲突检查。
- HTML 仍支持 25 MB 导入与内嵌/外部图片附件化。

## 0.1.0 — 2026-09-17

首个本地试用版：

- 普通静态 HTML 导入；点击实际元素就地留言，无需预设模块。
- 本机 Codex 执行单条或批量留言，支持样式及文字修改、逐条效果对照。
- 采用后保存/导出、撤销/重做、保存历史和自动草稿恢复。
- 留言暂存、冲突检查、多窗口草稿隔离和存储失败提示。
- Mac 双击启动入口、已捆绑生产依赖的 ZIP 试用包。

已在 macOS、Node.js 24.21.0、Chrome 和 Codex CLI 0.153.4 环境验证。其他操作系统及全部 Codex provider 尚未覆盖；本版不是 Obsidian 插件或任意网站编辑器。
